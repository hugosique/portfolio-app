import { FloatingSquareDirective } from './floating-square.directive';

describe('FloatingSquareDirective', () => {
  it('should create an instance', () => {
    const directive = new FloatingSquareDirective();
    expect(directive).toBeTruthy();
  });
});
